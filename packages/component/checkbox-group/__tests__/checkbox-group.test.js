'use strict';

const checkboxGroup = require('..');
const assert = require('assert').strict;

assert.strictEqual(checkboxGroup(), 'Hello from checkboxGroup');
console.info('checkboxGroup tests passed');
